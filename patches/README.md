ZeroMount SUSFS coupling patches. Applied to upstream susfs4ksu at the
commit pinned in ../susfs-version.txt during CI (B01, S01, S02).

Patch files must be named zeromount-*.patch to be picked up by CI.
