# Claude session data (NOT commands/hooks — those are tracked)
.claude/handoff.md
.claude/progress.json

# Environment secrets
.env
.env.*

# Credentials
*.pem
*.key
*.jks
*.keystore
credentials.json
service-account*.json

# MCP config
.mcp.json

# Database files
*.db
*.db-shm
*.db-wal

# Log files
logs_llm/

# Build artifacts
build/
node_modules/
.gradle/
*.apk
*.zip

# IDE
.idea/
.vscode/
*.iml

# Rust build artifacts
target/

# Tool/analysis data
.audit/
.mcp-vector-search/

# Reference repos (not part of this project)
susfs4ksu-new/
susfs4ksu-module/
BRENE/
/zeromount/

# Release artifacts
release/

# Documentation (project-internal, not shipped)
docs/
CLAUDE.md

# WebUI source deps + lockfile (rebuilt from package.json)
webui/node_modules/
webui/package-lock.json

# WebUI build output (rebuilt by vite build)
module/webroot/

# Compiled binaries (rebuilt by package.sh)
module/bin/
module/zeromount

# External tools (not part of core project)
tools/
